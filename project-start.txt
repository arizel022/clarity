


в папку проекта из стартовой сборки копируем
app
gulpfile.js
package.json






СБОРКА----------------66666666666666666666666666666666666666666

// gulp -v проверка версии
// --------------------------------
// ставим node.js

// очищаем старый галп

// npm rm --global gulp

// ставим галп глобально
// npm install --global gulp-cli

// открываем галп локально в папке стартовой сборки и 
// npm init 
// проходим вопросы по данным проекта

// ставим галп локально в папку стартовой сборки
// npm i --save-dev gulp
// появляется папка node_modules

// создаем app и dist

// теперь нам нужно установить плагин от галпа, который будет конвертировать scss/sass в обычный css
// npm i --save-dev gulp-sass

// дальше начинаем заполнять gulpfile, создавая константы. Имя константы может быть любое. Мы создаем константу scss, под капотом которой будет конвертация scss и sass в обычный css. С помощью require мы присваиваем константе ВСЕ возможности плагина, в данном случае 'gulp-sass'.
//дальше для выполнения ранее мы писали таски для галпа. Теперь можно писать просто функции. Создадим функцию styles, которая будет отвечать за наши стили. Создал scss/style.scss, прописал src. Теперь добавил src в качестве константы - для нее будет работать просто  'gulp'.
//устанавливаю галп-конкат для объединения файлов и переименования в min
// npm i --save-dev gulp-concat
// дальше надо смотреть по видео((



